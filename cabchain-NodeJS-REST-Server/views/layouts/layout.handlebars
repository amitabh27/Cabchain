<!DOCTYPE html>
<html>
<head>
	<title>Cabchain</title>
	<link rel="stylesheet" href="/css/bootstrap.css" />
	<link rel="stylesheet" href="/css/style.css" />
</head>
<body background="img.jpg">
	<div class="container">
      <div class="header clearfix">
        <nav>
          <ul class="nav nav-pills pull-right">
            {{#if user}}
              <li role="presentation"><a href="/users/logout">Logout</a></li>
            {{else}}
              <li role="presentation"><a href="/users/register">Join Cabchain</a></li>
			  <li role="presentation"><a href="/users/contact">Contact Us</a></li>
            {{/if}}
          </ul>
        </nav>
        <h3 class="text-muted">Cabchain</h3>
      </div>


      <div class="row">
        <div class="col-lg-12">
          {{#if success_msg}}
            <div class="alert alert-success">{{success_msg}}</div>
          {{/if}}

          {{#if error_msg}}
            <div class="alert alert-danger">{{error_msg}}</div>
          {{/if}}

          {{#if error}}
            <div class="alert alert-danger">{{error}}</div>
          {{/if}}
          
          {{{body}}}
        </div>

        
      </div>

      <footer class="footer">
        <p>&copy; 2018 Cabchain, Inc.</p>
      </footer>

    </div> <!-- /container -->
</body>
</html>
