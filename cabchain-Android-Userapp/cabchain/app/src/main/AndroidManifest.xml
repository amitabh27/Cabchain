<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kulka.cabchain">

    <uses-sdk android:minSdkVersion="4" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewUserActivity" />
        <activity android:name=".RegUserActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_
             maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".kfnakfka"
            android:label="@string/title_activity_kfnakfka"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".QuoteStart"
            android:label="@string/title_activity_quote_start"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewQuoteStart"
            android:label="@string/title_activity_new_quote_start"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RideStart"
            android:label="@string/title_activity_ride_start"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Riding"
            android:label="@string/title_activity_riding"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Ratings"></activity>
    </application>

</manifest>